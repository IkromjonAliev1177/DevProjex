<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DevProjex.Avalonia.ViewModels"
             x:Class="DevProjex.Avalonia.Views.AboutPopoverView"
             x:DataType="vm:MainWindowViewModel">
  <Border Classes="theme-popover"
          Padding="14"
          Width="600"
          MinWidth="456"
          MaxWidth="{Binding AboutPopoverMaxWidth}"
          MinHeight="288"
          MaxHeight="{Binding AboutPopoverMaxHeight}">
    <Grid RowDefinitions="Auto,*,Auto">
      <Grid ColumnDefinitions="*,Auto">
        <TextBlock Text="{Binding HelpAboutTitle}" FontSize="15" FontWeight="SemiBold" />
        <Button Grid.Column="1"
                Content="✕"
                Classes="menu-icon-button"
                Padding="4"
                Click="OnClose" />
      </Grid>

      <ScrollViewer Grid.Row="1" Margin="0,12,0,12"
                    IsScrollInertiaEnabled="True"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
        <TextBlock Text="{Binding HelpAboutBody}"
                   TextWrapping="Wrap"
                   FontSize="15" />
      </ScrollViewer>

      <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
        <Button Content="{Binding HelpAboutOpenLink}" Classes="primary-action" Click="OnOpenLink" />
        <Button Content="{Binding HelpAboutCopyLink}" Classes="primary-action" Click="OnCopyLink" />
      </StackPanel>
    </Grid>
  </Border>
</UserControl>
